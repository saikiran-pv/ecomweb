<h2>All Orders</h2>

<% @orders.each do |order| %>

Hi <%= current_user.name %>
  <p>Line Items: </p>
  <ul>
    <% order.line_items.each do |item| %>
    <li><%= item.quantity %> x <%= item.product.product_name %></li>
    <% end %>
  </ul>
<% end %>